import 'package:get/get.dart';

class VehicleController extends GetxController {
  final truckUnit = 'Not assigned'.obs;
  final truck = 'Not assigned'.obs;
  final truckVin = 'Not assigned'.obs;
  final truckPlate = 'Not assigned'.obs;
  final trailerUnit = 'Not assigned'.obs;
  final trailerType = 'Not assigned'.obs;
}
